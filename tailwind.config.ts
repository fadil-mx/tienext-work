import { type Config } from 'tailwindcss'

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        albert: ['var(--font-albert-sans)'],
      },
    },
  },
  plugins: [],
}
export default config
